#include<vector>

#include<random_graph_generator.hpp>
#include<typedefs.hpp>

using namespace std;

int main(int argc,char* argv[]){
    /* Por parametros de invocacion del programa me (deben) llega(n/r):
     * n = argv[1]
     * cant_por_tam = argv[2]
     * cant_ejec_por_inst = argv[3]
     * archivo de salida = argv[4]
     */

    /* Variables locales */
    //std::ofstream archivo;
    //clock_t t;
    //unsigned int cant_por_tam = (unsigned int)std::stoi(argv[2]);
    //unsigned int cant_ejec_por_inst = (unsigned int)std::stoi(argv[3]);

    /* Abro el archivo (4to parametro) */
    //archivo.open(argv[4],std::ios::app);

    return 0;
}
